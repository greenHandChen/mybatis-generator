spring:
  application:
    name: mybatis_generator
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://172.23.12.110:3306/mybatis_test?useUnicode=true&characterEncoding=utf8&useSSL=true
    username: root
    password: root

cux:
  mybatis:
    mapper-locations: classpath:mybatis/mapper/*.xml # nameSpace必须与mapper的接口名一致
    configuration:
      mapUnderscoreToCamelCase: true

server:
  port: 9111